# Active Directory
ldap.url=ldap://10.83.3.30:3268/dc=groupinfra,dc=com
ldap.user=CN=SA-FR-LDAP_Query,OU=Service Accounts,OU=FR,OU=Landlord FR,OU=Corporate,DC=groupinfra,DC=com
ldap.password=ywxZFHoyTEsL1

# DataSource settings
spring.datasource.url = jdbc:mysql://localhost:3306/cgihosting
spring.datasource.username = root
spring.datasource.password = Vtsg13j1

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
#spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the domain manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.jpa.hibernate.naming.strategy=org.hibernate.cfg.EJB3NamingStrategy